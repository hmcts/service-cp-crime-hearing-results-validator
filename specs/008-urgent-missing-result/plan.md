# Implementation Plan: URGENT Result Missing Warning (CRA-22 / DR-URG-008)

**Branch**: `CRA-22-urgent-missing-result` | **Date**: 2026-09-02 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/008-urgent-missing-result/spec.md`

## Summary

Add validation rule **DR-URG-008** that warns Crown Court users when all of a defendant's conditional-bail offences have been resulted with bail-ending outcomes (final result Category F, sentence deferred DS, remand-in-custody family, or warrant without bail WOFN) but no URGENT result has been recorded. The warning fires at **defendant level** with the fixed message: _"The defendant's conditional bail has ended. You may need to add the URGENT result and select "Bail conditions cancelled" on one of the offences before sharing."_

Following Constitution Principle I (YAML/CEL Rule-First), the rule is expressed entirely in `DR-URG-008.yaml`. A new `ConditionalBailPreprocessor` + `ConditionalBailContext` pair handles the defendant-grouped, offence-remand-status-aware preprocessing. The feature is blocked on CHD-2485, which delivers `OffenceDto.getRemandStatus()` from the upstream `api-cp-crime-hearing-results-validator` library.

## Technical Context

**Language/Version**: Java 25
**Primary Dependencies**: Spring Boot 4, `org.projectnessie.cel` (CEL engine), Caffeine, `api-cp-crime-hearing-results-validator` library (bumped to CHD-2485 version)
**Storage**: PostgreSQL 15.3 — one new `validation_rule` row via Flyway migration V1.009
**Testing**: JUnit 5 + Mockito + AssertJ (unit), TestContainers + WireMock (integration), RestTemplate (API live)
**Target Platform**: Azure-hosted Spring Boot microservice, port 4550
**Project Type**: REST API validation service
**Performance Goals**: Sub-millisecond rule evaluation per defendant (CEL expressions are cached)
**Constraints**: `gradle build` must pass Checkstyle Google (`maxWarnings = 0`) + PMD (`ignoreFailures = false`)
**Scale/Scope**: One new YAML rule; one new preprocessor + context pair; one Flyway migration; four test files

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| I — YAML/CEL Rule-First | ✅ PASS | `DR-URG-008.yaml` is created first; all rule logic (CEL expression, severity, message, short code lists) lives in YAML |
| II — Constructor Injection & Immutable DTOs | ✅ PASS | `ConditionalBailPreprocessor` uses `@Component` with no fields (stateless); `ConditionalBailContext` is a Java record |
| III — Layered Architecture & Registry Dispatch | ✅ PASS | Preprocessor registered via `@Component` with `type()` qualifier `"conditional-bail-urgent-check"`; `PreprocessorRegistry` picks it up automatically |
| IV — Spec-Driven Build Loop | ✅ PASS | Spec → Plan → Tasks → Implement → Code Review → QA → Spec Validate |
| V — HMCTS Standards | ✅ PASS | Java 25, Spring Boot 4, Gradle, SLF4J, no Maven |
| VI — Severity Ceiling, Never Promote | ✅ PASS | YAML severity is `WARNING` (the highest this rule can ever produce); DB override can only lower it |
| VII — No System.out / SLF4J Only | ✅ PASS | All logging via `@Slf4j` / `LoggerFactory` |
| VIII — TDD | ✅ PASS | Tests written before production code (enforced by quickstart step ordering) |

**Re-check after Phase 1 design**: All principles confirmed compliant — no deviations.

## Project Structure

### Documentation (this feature)

```text
specs/008-urgent-missing-result/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0 — decisions & rationale
├── data-model.md        # Phase 1 — entities, fields, YAML schema
├── quickstart.md        # Phase 1 — TDD implementation guide
├── checklists/
│   └── requirements.md  # Spec quality checklist (all pass)
└── tasks.md             # Phase 2 — generated by /speckit-tasks
```

### Source Code (repository root)

```text
src/main/resources/rules/
└── DR-URG-008.yaml                                              [NEW]

src/main/java/uk/gov/hmcts/cp/services/rules/cel/
├── ConditionalBailPreprocessor.java                             [NEW]
├── ConditionalBailContext.java                                  [NEW]
└── PreprocessingDefinition.java                                 [MODIFY: +bailEndingShortCodes, +conditionalBailRemandStatus]

src/main/resources/db/migration/
└── V1.009__insert_dr_urg_008.sql                                [NEW]

src/test/java/uk/gov/hmcts/cp/services/rules/cel/
├── ConditionalBailContextTest.java                              [NEW]
└── ConditionalBailPreprocessorTest.java                         [NEW]

src/test/java/uk/gov/hmcts/cp/integration/
└── UrgentMissingWarningIntegrationTest.java                     [NEW]

src/apiTest/java/uk/gov/hmcts/cp/
└── UrgentMissingWarningApiHttpLiveTest.java                     [NEW]
```

**Structure decision**: Single-project layout (existing pattern). Back-end only — no front-end changes. All new files follow the established co-location of preprocessor + context + YAML rule.

## Key Design Decisions

### Preprocessing: defendant-grouped with per-offence remand status lookup

One `ConditionalBailContext` is produced per deduplicated defendant (using `PreprocessorHelper.groupLinesByDedupedDefendant()`). For each defendant, conditional-bail offences are identified via `OffenceDto.getRemandStatus()`. Result lines are grouped by offence to check for bail-ending results and URGENT.

**CEL context variables:**

| Variable                    | Type | CEL expression role                                    |
|-----------------------------|------|--------------------------------------------------------|
| `conditionalBailOffenceCount` | Long | Guard: > 0 means at least one conditional-bail offence |
| `bailEndedCount`            | Long | Must equal `conditionalBailOffenceCount` for "all ended" |
| `hasUrgentCount`            | Long | Must be 0 for warning to fire                          |

**CEL expression:**
```
conditionalBailOffenceCount > 0
  && bailEndedCount == conditionalBailOffenceCount
  && hasUrgentCount == 0
```

### Bail-ending detection: Category F OR explicit short codes

A result line is bail-ending if `category == F` OR `shortCode ∈ {DS, RI, RIYDA, RIH, RIB, RILA, RILAB, REMYD, WOFN}`. Both paths are needed because AC1 (final result) uses Category F while AC2 (DS), AC3 (RI family), and AC4 (WOFN) use non-F short codes.

### URGENT detection: hardcoded constant

`"URGENT"` is hardcoded as a private constant in `ConditionalBailPreprocessor`. It is not YAML-configurable — it is a stable business constant that does not vary.

### CHD-2485 null-safety: graceful no-op before delivery

When `offence.getRemandStatus()` is null (library version < CHD-2485), the preprocessor skips that offence. `conditionalBailOffenceCount` remains 0, the CEL expression is false, and the rule never fires. Zero false positives.

## External Dependency

| Item | Detail |
|------|--------|
| Library | `api-cp-crime-hearing-results-validator` |
| Current version | 26.25 (does NOT have `OffenceDto.getRemandStatus()`) |
| Required version | TBD — delivered by CHD-2485 |
| Blocker | Implementation is complete-able and testable, but rule is a no-op in production until CHD-2485 ships and the library version is bumped in `build.gradle` |

## Complexity Tracking

No constitution violations. No deviations from standard patterns.

## Do Not Add

- Per-offence issue output — this rule is defendant-level only (AC7 of CRA-22 is front-end).
- UI rendering changes — out of scope for this service.
- Override / severity-ceiling integration test — already proven in `ValidationRuleOverrideIntegrationTest` (design_rules.md constraint).
- AC6 back-end changes — the existing framework already supports mixed offence/defendant warnings.
