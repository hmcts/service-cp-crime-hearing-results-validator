# Default server port configuration
# Can be overridden by setting SERVER_PORT environment variable
server.port=8082

# Project version — base for main-branch / non-release artefact versions.
# Release builds use the GitHub release tag instead.
projectVersion=1.0.0
